import React, { useState } from "react";
// import API from "../../utils/API";
import "./style.css";

function Table(props) {
    // in here you want to render the table names and columns, not the actual table
    // table header columns include Photo, Name (first / last), Email, Phone, City/State
    // info to sort by first name, last name, city
    
}

export default Table()